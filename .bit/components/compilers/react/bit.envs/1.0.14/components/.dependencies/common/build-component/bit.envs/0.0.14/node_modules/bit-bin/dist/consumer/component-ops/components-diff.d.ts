import { Consumer } from '..';
import { BitId } from '../../bit-id';
declare type FileDiff = {
    filePath: string;
    diffOutput: string;
};
export declare type FieldsDiff = {
    fieldName: string;
    diffOutput: string;
};
export declare type DiffResults = {
    id: BitId;
    hasDiff: boolean;
    filesDiff?: FileDiff[];
    fieldsDiff?: FieldsDiff[] | null | undefined;
};
declare const _default: (consumer: Consumer, ids: BitId[], version: string | null | undefined, toVersion: string | null | undefined, verbose: boolean) => Promise<DiffResults[]>;
export default _default;
