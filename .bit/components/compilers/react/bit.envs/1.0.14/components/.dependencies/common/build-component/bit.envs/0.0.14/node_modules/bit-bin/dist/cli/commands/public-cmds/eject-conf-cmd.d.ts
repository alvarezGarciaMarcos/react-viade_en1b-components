import Command from '../../command';
import { EjectConfResult } from '../../../consumer/component-ops/eject-conf';
import { PathOsBased } from '../../../utils/path';
declare type EjectConfCliResult = EjectConfResult & {
    ejectPathRelativeToCwd: PathOsBased;
};
export default class EjectConf extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    loader: boolean;
    migration: boolean;
    action([id]: [string], { path }: {
        path?: string;
    }): Promise<EjectConfCliResult>;
    report(ejectResults: EjectConfCliResult): string;
}
export {};
