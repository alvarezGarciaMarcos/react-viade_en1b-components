import Command from '../../command';
import ConsumerComponent from '../../../consumer/component/consumer-component';
import { DependenciesInfo } from '../../../scope/graph/scope-graph';
export default class Show extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    loader: boolean;
    migration: boolean;
    skipWorkspace: boolean;
    remoteOp: boolean;
    action([id]: [string], { json, versions, remote, outdated, compare, detailed, dependents, dependencies }: {
        json?: boolean;
        versions: boolean | null | undefined;
        remote: boolean;
        outdated?: boolean;
        compare?: boolean;
        detailed?: boolean;
        dependents?: boolean;
        dependencies?: boolean;
    }): Promise<any>;
    report({ component, componentModel, dependenciesInfo, dependentsInfo, json, versions, components, outdated, detailed }: {
        component: ConsumerComponent;
        componentModel?: ConsumerComponent;
        dependenciesInfo: DependenciesInfo[];
        dependentsInfo: DependenciesInfo[];
        json: boolean | null | undefined;
        versions: boolean | null | undefined;
        components: ConsumerComponent[] | null | undefined;
        outdated: boolean;
        detailed: boolean;
    }): string;
}
