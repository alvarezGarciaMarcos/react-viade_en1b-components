"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = isBitUrl;

function isBitUrl(url) {
  const regex = new RegExp('((bit|ssh|http(s)?)|(bit@[w.]+))(:(//)?)([w.@:/-~]+)(.bit)(/)?');
  return regex.test(url);
}