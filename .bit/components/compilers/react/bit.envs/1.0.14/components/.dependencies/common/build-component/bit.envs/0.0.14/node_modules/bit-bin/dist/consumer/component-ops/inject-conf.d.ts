import ConsumerComponent from '../component/consumer-component';
import BitMap from '../bit-map';
import ConfigDir from '../bit-map/config-dir';
export declare type InjectConfResult = {
    id: string;
};
declare const _default: (component: ConsumerComponent, consumerPath: string, bitMap: BitMap, configDir: ConfigDir, force?: boolean | undefined) => Promise<InjectConfResult>;
export default _default;
