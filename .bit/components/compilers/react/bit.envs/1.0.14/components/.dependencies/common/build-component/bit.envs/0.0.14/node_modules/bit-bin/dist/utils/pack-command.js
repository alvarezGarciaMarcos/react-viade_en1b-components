"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

function _toBase() {
  const data = _interopRequireDefault(require("./string/to-base64"));

  _toBase = function () {
    return data;
  };

  return data;
}

module.exports = function packCmd(obj, base64 = true) {
  if (base64) {
    return (0, _toBase().default)(JSON.stringify(obj));
  }

  return JSON.stringify(obj);
};