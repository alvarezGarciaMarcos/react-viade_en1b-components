import Command from '../../command';
import { BitId } from '../../../bit-id';
import { EjectResults } from '../../../consumer/component-ops/eject-components';
export default class Export extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    loader: boolean;
    migration: boolean;
    remoteOp: boolean;
    action([remote, ids]: [string, string[]], { eject, includeDependencies, setCurrentScope, all, force, rewire }: any): Promise<any>;
    report({ componentsIds, nonExistOnBitMap, missingScope, ejectResults, remote, includeDependencies }: {
        componentsIds: BitId[];
        nonExistOnBitMap: BitId[];
        missingScope: BitId[];
        ejectResults: EjectResults | null | undefined;
        remote: string;
        includeDependencies: boolean;
    }): string;
}
