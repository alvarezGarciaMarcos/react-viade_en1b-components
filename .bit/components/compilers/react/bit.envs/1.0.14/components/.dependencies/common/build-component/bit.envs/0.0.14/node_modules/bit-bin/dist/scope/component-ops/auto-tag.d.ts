import ModelComponent from '../models/model-component';
import { BitIds } from '../../bit-id';
import Scope from '../scope';
import Component from '../../consumer/component/consumer-component';
import { Version } from '../models';
export declare type AutoTagResult = {
    component: ModelComponent;
    triggeredBy: BitIds;
    version: Version;
    versionStr: string;
};
export declare function bumpDependenciesVersions(scope: Scope, potentialComponents: BitIds, taggedComponents: Component[]): Promise<AutoTagResult[]>;
export declare function getAutoTagPending(scope: Scope, potentialComponents: BitIds, changedComponents: BitIds): Promise<ModelComponent[]>;
