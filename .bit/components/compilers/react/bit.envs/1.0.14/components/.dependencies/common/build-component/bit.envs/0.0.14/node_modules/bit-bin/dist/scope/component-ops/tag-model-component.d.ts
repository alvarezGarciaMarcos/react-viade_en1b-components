import { Scope } from '..';
import Consumer from '../../consumer/consumer';
import Component from '../../consumer/component/consumer-component';
import { AutoTagResult } from './auto-tag';
declare const _default: ({ consumerComponents, scope, message, exactVersion, releaseType, force, consumer, ignoreNewestVersion, skipTests, verbose, skipAutoTag }: {
    consumerComponents: Component[];
    scope: Scope;
    message: string;
    exactVersion: string | null | undefined;
    releaseType: any;
    force: boolean | null | undefined;
    consumer: Consumer;
    ignoreNewestVersion: boolean;
    skipTests: boolean;
    verbose?: boolean | undefined;
    skipAutoTag: boolean;
}) => Promise<{
    taggedComponents: Component[];
    autoTaggedResults: AutoTagResult[];
}>;
export default _default;
