import { Pojo, StateArguments } from './types';
export interface StateResult {
    res: string;
    errors: string[] | null;
}
export declare type SubValidator = (value: string, ...rest: string[]) => StateResult;
export interface StateParamType {
    subValidators?: Pojo<SubValidator>;
    validate(value: any, args: StateArguments, resolveParam: any, validateDefinition: boolean, validateValue: boolean): StateResult;
}
export declare const systemValidators: Pojo<StateParamType>;
