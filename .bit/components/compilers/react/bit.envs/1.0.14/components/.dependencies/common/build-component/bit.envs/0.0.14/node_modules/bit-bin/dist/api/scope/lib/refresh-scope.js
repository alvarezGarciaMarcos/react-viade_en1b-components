"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = refreshScope;

function _scope() {
  const data = require("../../../scope");

  _scope = function () {
    return data;
  };

  return data;
}

function refreshScope(path) {
  return (0, _scope().loadScope)(path).then(scope => {
    return scope.objects.list().then(objects => Promise.all(objects.map(o => scope.objects._writeOne(o))));
  });
}