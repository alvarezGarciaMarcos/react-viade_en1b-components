"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

function _runAndUpdateCi() {
  const data = _interopRequireDefault(require("./run-and-update-ci"));

  _runAndUpdateCi = function () {
    return data;
  };

  return data;
}

// @ts-ignore AUTO-ADDED-AFTER-MIGRATION-PLEASE-FIX!
const scopePath = process.env.__scope__;
const id = process.env.__id__;

if (!id) {
  throw new Error('id for ci-worker must be provided');
}

(0, _runAndUpdateCi().default)({
  id,
  scopePath
}).then(() => null).catch(er => process.stderr.write(er));