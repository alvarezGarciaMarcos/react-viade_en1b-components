import Command from '../../command';
import ModelComponent from '../../../scope/models/model-component';
export default class CatScope extends Command {
    name: string;
    description: string;
    private: boolean;
    alias: string;
    opts: string[][];
    action([scopePath]: [string], { full, json, jsonExtra }: {
        full: boolean | null | undefined;
        json: boolean | null | undefined;
        jsonExtra: boolean | null | undefined;
    }): Promise<any>;
    report({ payload, full, json, jsonExtra }: {
        payload: ModelComponent[];
        full: boolean | null | undefined;
        json: boolean | null | undefined;
        jsonExtra: boolean | null | undefined;
    }): string;
}
