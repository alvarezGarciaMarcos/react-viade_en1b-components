import Command from '../../command';
export default class ClearCache extends Command {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    loader: boolean;
    skipWorkspace: boolean;
    action(): Promise<any>;
    report(): string;
}
