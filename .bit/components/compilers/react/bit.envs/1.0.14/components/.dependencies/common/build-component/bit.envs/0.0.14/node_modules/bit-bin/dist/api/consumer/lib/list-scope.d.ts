import { ListScopeResult } from '../../../consumer/component/components-list';
import { BitId } from '../../../bit-id';
import { SSHConnectionStrategyName } from '../../../scope/network/ssh/ssh';
export declare function listScope({ scopeName, showAll, showRemoteVersion, namespacesUsingWildcards, strategiesNames }: {
    scopeName?: string;
    showAll?: boolean;
    showRemoteVersion?: boolean;
    namespacesUsingWildcards?: string;
    strategiesNames?: SSHConnectionStrategyName[];
}): Promise<ListScopeResult[]>;
export declare function getRemoteBitIdsByWildcards(idStr: string): Promise<BitId[]>;
