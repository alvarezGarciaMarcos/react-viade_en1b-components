import Command from '../../command';
declare class ScopeConfigGet extends Command {
    name: string;
    description: string;
    alias: string;
    private: boolean;
    opts: never[];
    action([key]: [string]): Promise<any>;
    report(value: string): string;
}
declare class ScopeConfigList extends Command {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    action(): Promise<any>;
    report(conf: {
        [key: string]: any;
    }): string;
}
declare class ScopeConfigDel extends Command {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    action([key]: [string]): Promise<any>;
    report(): string;
}
declare class ScopeConfigSet extends Command {
    name: string;
    description: string;
    alias: string;
    private: boolean;
    opts: never[];
    action([key, value]: [string, string]): Promise<any>;
    report({ key, value }: {
        key: string;
        value: number;
    }): string;
}
export default class ScopeConfig extends Command {
    name: string;
    description: string;
    alias: string;
    commands: (ScopeConfigGet | ScopeConfigList | ScopeConfigDel | ScopeConfigSet)[];
    opts: never[];
    migration: boolean;
    action(): Promise<any>;
    report(conf: {
        [key: string]: string;
    }): string;
}
export {};
