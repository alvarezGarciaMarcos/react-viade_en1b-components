import Command from '../../command';
export default class Logout extends Command {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    action(): Promise<any>;
    report(): string;
}
