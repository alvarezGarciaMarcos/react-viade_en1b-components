export default function buildInScope({ id, save, verbose, scopePath, directory, keep, noCache }: {
    id: string;
    save?: boolean;
    verbose?: boolean;
    scopePath?: string;
    directory?: string;
    keep: boolean;
    noCache: boolean;
}): any;
