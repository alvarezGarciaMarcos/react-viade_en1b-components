import { PathOsBased } from '../utils/path';
declare function componentResolver(componentId: string, mainFilePath: string | null | undefined, projectRoot?: PathOsBased): PathOsBased;
export default componentResolver;
