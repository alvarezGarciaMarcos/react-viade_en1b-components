import Command from '../../command';
import { ApplyVersionResults } from '../../../consumer/versions-ops/merge-version';
export default class Checkout extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    loader: boolean;
    action([values]: [string[]], { interactiveMerge, ours, theirs, manual, reset, all, verbose, skipNpmInstall, ignoreDist }: {
        interactiveMerge?: boolean;
        ours?: boolean;
        theirs?: boolean;
        manual?: boolean;
        reset?: boolean;
        all?: boolean;
        verbose?: boolean;
        skipNpmInstall?: boolean;
        ignoreDist?: boolean;
    }): Promise<ApplyVersionResults>;
    report({ components, version, failedComponents }: ApplyVersionResults): string;
}
