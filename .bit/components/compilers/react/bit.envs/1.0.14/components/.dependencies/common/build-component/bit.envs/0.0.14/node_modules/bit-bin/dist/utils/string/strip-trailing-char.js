"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = stripTrailingChar;

function stripTrailingChar(str, char) {
  if (!str || !char) {
    return str;
  }

  if (str[str.length - 1] === char) {
    return str.slice(0, -1);
  }

  return str;
}