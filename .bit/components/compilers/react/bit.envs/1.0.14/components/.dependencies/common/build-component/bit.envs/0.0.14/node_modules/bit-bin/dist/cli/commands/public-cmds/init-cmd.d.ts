import Command from '../../command';
export default class Init extends Command {
    name: string;
    skipWorkspace: boolean;
    description: string;
    alias: string;
    opts: string[][];
    action([path]: [string], flags: Record<string, any>): Promise<{
        [key: string]: any;
    }>;
    report({ created, bare, reset, resetHard }: any): string;
}
