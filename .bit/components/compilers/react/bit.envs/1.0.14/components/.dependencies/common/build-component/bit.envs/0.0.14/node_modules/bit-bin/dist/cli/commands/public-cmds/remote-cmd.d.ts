import Command from '../../command';
declare class RemoteAdd extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    action([url]: [string], { global }: {
        global: boolean;
    }): Promise<any>;
    report({ name, host }: {
        name: string;
        host: string;
    }): string;
}
declare class RemoteRm extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    action([name]: [string], { global }: {
        global: boolean;
    }): Promise<any>;
    report(name: string): string;
}
export default class Remote extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    migration: boolean;
    commands: (RemoteAdd | RemoteRm)[];
    action(args: string[], { global }: {
        glboal: boolean;
    }): Promise<any>;
    report(remotes: {
        [key: string]: string;
    }): string;
}
export {};
