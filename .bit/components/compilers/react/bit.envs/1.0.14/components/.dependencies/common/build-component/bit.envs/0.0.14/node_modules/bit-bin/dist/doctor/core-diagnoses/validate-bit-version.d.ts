import Diagnosis from '../diagnosis';
import { ExamineBareResult } from '../diagnosis';
export default class ValidateBitVersion extends Diagnosis {
    name: string;
    description: string;
    category: string;
    _formatSymptoms(bareResult: ExamineBareResult): string;
    _formatManualTreat(bareResult: ExamineBareResult): "please make sure you have an internet connection" | "please upgrade your bit version";
    _runExamine(): Promise<ExamineBareResult>;
}
