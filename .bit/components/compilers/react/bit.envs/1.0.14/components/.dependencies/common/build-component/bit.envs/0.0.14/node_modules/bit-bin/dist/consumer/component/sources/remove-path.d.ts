export default class RemovePath {
    path: string;
    removeItsDirIfEmpty: boolean;
    constructor(path: string, removeItsDirIfEmpty?: boolean | undefined);
    persistToFS(): Promise<boolean | void>;
}
