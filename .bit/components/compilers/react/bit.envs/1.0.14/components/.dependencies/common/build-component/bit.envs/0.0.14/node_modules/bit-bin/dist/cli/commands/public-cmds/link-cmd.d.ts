import Command from '../../command';
export default class Create extends Command {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    private: boolean;
    loader: boolean;
    action(): Promise<any>;
    report(results: Array<{
        id: string;
        bound: Record<string, any> | null | undefined;
    }>): string;
}
