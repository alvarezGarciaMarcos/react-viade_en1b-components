import GitHook from './git-hook';
export default class GitHooksManager {
    basePath: string;
    hooks: Map<string, GitHook>;
    constructor(basePath: string);
    readonly hooksDirPath: string;
    static init(basePath: string): GitHooksManager;
    writeAllHooks(): {
        added: never[];
        alreadyExist: never[];
    };
    static getInstance(): GitHooksManager | null | undefined;
}
