import Diagnosis from '../diagnosis';
import { ExamineBareResult } from '../diagnosis';
export declare const DIAGNOSIS_NAME = "validate git exec";
export default class ValidateGitExec extends Diagnosis {
    name: string;
    description: string;
    category: string;
    _formatSymptoms(bareResult: ExamineBareResult): string;
    _formatManualTreat(): string;
    _runExamine(): Promise<ExamineBareResult>;
}
