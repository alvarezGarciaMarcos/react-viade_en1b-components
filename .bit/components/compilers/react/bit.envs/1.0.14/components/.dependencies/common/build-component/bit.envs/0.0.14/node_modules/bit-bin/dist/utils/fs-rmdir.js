"use strict";

var _interopRequireWildcard = require("@babel/runtime/helpers/interopRequireWildcard");

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = rmDir;

function _fsExtra() {
  const data = _interopRequireDefault(require("fs-extra"));

  _fsExtra = function () {
    return data;
  };

  return data;
}

function pathlib() {
  const data = _interopRequireWildcard(require("path"));

  pathlib = function () {
    return data;
  };

  return data;
}

function rmDir(path) {
  if (_fsExtra().default.existsSync(path)) {
    _fsExtra().default.readdirSync(path).forEach(file => {
      const curPath = pathlib().join(path, file);

      if (_fsExtra().default.lstatSync(curPath).isDirectory()) {
        rmDir(curPath);
      } else {
        _fsExtra().default.unlinkSync(curPath);
      }
    });

    _fsExtra().default.rmdirSync(path);
  }
}