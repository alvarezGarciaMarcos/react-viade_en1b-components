import Command from '../../command';
declare class ResolverSet extends Command {
    name: string;
    description: string;
    alias: string;
    private: boolean;
    opts: never[];
    action([resolverPath]: [string]): Promise<any>;
    report(resolverPath: any): string;
}
declare class ResolverReset extends Command {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    action(): Promise<any>;
    report(): string;
}
export default class Resolver extends Command {
    name: string;
    description: string;
    alias: string;
    opts: never[];
    commands: (ResolverSet | ResolverReset)[];
    private: boolean;
    action(): Promise<any>;
    report(resovlerPath: string): string;
}
export {};
