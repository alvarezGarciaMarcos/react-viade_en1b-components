"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = cleanBang;

function _cleanChar() {
  const data = _interopRequireDefault(require("./clean-char"));

  _cleanChar = function () {
    return data;
  };

  return data;
}

/**
 * remove first bang (!) from `str`
 * @name cleanBang
 * @param {string} str string to manipulate
 * @returns {string} string without first found bang
 * @example
 * ```js
 *  cleanBang('!bang') // =>  'bang'
 * ```
 */
function cleanBang(str) {
  return (0, _cleanChar().default)(str, '!');
}