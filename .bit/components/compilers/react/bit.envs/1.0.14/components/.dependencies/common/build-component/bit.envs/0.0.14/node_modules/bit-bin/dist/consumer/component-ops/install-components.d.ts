import { Consumer } from '..';
import { BitId } from '../../bit-id';
import { LinksResult } from '../../links/node-modules-linker';
export declare function install(consumer: Consumer, verbose: boolean): Promise<LinksResult[]>;
export declare function installIds(consumer: Consumer, ids: BitId[], verbose: boolean): Promise<LinksResult[]>;
