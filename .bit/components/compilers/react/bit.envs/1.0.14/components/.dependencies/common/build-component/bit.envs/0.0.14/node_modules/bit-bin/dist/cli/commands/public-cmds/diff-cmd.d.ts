import Command from '../../command';
import { DiffResults } from '../../../consumer/component-ops/components-diff';
export default class Diff extends Command {
    name: string;
    description: string;
    alias: string;
    opts: string[][];
    loader: boolean;
    action([values]: [string[]], { verbose }: {
        verbose?: boolean;
    }): Promise<DiffResults[]>;
    report(diffResults: DiffResults[]): string;
}
