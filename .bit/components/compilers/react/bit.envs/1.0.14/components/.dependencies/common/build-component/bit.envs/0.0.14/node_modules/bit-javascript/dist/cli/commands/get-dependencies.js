"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

function _actions() {
  const data = require("../../actions");

  _actions = function () {
    return data;
  };

  return data;
}

function _constants() {
  const data = require("../../constants");

  _constants = function () {
    return data;
  };

  return data;
}

// @flow
const report = data => {
  if (!data) return 'No dependencies found!';
  return JSON.stringify(data, null, '  ');
};

const resolveConfig = undefined; // @todo: figure out how to get the data from the command line, maybe as a file

const getDependenciesCommand = {
  name: 'get-dependencies <baseDir> <file>',
  description: 'get dependencies list of a file',
  action: (baseDir, file) => (0, _actions().getDependenciesAction)(baseDir, file, _constants().DEFAULT_BINDINGS_PREFIX, resolveConfig),
  report,
  // @ts-ignore AUTO-ADDED-AFTER-MIGRATION-PLEASE-FIX!
  loaderText: 'Finding dependencies',
  loader: true
};
var _default = getDependenciesCommand;
exports.default = _default;