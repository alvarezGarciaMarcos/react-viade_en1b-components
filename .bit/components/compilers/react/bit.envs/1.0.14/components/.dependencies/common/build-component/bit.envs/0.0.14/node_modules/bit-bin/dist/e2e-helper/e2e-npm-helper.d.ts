import CommandHelper from './e2e-command-helper';
import FsHelper from './e2e-fs-helper';
import ScopesData from './e2e-scopes';
export default class NpmHelper {
    command: CommandHelper;
    fs: FsHelper;
    scopes: ScopesData;
    constructor(scopes: ScopesData, fsHelper: FsHelper, commandHelper: CommandHelper);
    initNpm(initPath?: string): void;
    installNpmPackage(name: string, version: string | null | undefined, cwd?: string): string;
    addNpmPackage(name?: string, version?: string): void;
}
