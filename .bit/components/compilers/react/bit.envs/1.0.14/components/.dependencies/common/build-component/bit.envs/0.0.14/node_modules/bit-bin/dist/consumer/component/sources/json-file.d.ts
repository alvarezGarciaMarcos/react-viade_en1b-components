import AbstractVinyl from './abstract-vinyl';
export default class JSONFile extends AbstractVinyl {
    override: boolean;
    write(): Promise<string>;
    static load({ base, path, content, override }: {
        base: string;
        path: string;
        content: Record<string, any>;
        override?: boolean;
    }): JSONFile;
}
