"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getWithoutExt;

function _getExt() {
  const data = _interopRequireDefault(require("./get-ext"));

  _getExt = function () {
    return data;
  };

  return data;
}

function getWithoutExt(filename) {
  const ext = (0, _getExt().default)(filename); // There is no extension just return the file name

  if (ext === filename) {
    return filename;
  }

  return filename.substring(0, filename.length - ext.length - 1); // -1 to remove the '.'
}